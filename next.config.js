/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // images: {
  //   domains: ['https://portfolio-nextjs-five-alpha.vercel.app/'],
  // },
};

module.exports = nextConfig;
